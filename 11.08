// Создайте стрелочную функцию, которая бы принимала массив и цифру,
//  возвращала бы элемент массива под этим индексом.

const getIndex = (arr, index) => arr[index];




const array = [10, 20, 30, 40, 50];
console.log(getIndex(array, 2)); 



function decode(weatherCode) {
 
  const weatherDict = {
    SQ: "шквал",
    PO: "пыльный вихрь",
    FC: "торнадо",
    BR: "дымка (видимость от 1 до 9 км)",
    HZ: "мгла (видимость менее 10 км)",
    FU: "дым (видимость менее 10 км)",
    DS: "пыльная буря (видимость менее 10 км)",
    SS: "песчаная буря (видимость менее 10 км)",
  };

  if (weatherDict[weatherCode]) {
    return weatherDict[weatherCode];
  } else {
    return "Неизвестный код погоды";
  }
}







// // ЦИКЛЫ

// // 1. Вывести в консоль все числа от 0 до 20

for (let i = 0; i <= 20; i++) {
  console.log(i);
}

// // 2. Вывести в консоль каждое третье число от 15 до 96

for (let i = 15; i <= 96; i += 3) {
  console.log(i);
}

// // 3. Вывести в консоль каждое третье число от 96 до 15

for (let i = 96; i >= 15; i -= 3) {
  console.log(i);
}

// // 4. Вывести в консоль каждое число от 100 до 57
for (let i = 100; i >= 57; i--) {
  console.log(i);
}

const numbers = [6, 7, 1, -10, 78, 18, -5, -38, 9];

// // 2. Сформировать новый массив, в который
////попадут все числа из массива numbers умноженные на 2

const numbers2 = numbers.map((i) => i * 2);
console.log(numbers2);

// 3. Сформировать новый массив, в который попадут
// все положительные числа из массива numbers деленные на 2

const numbers3 = numbers.filter((i) => i > 0).map((i) => i / 2);
console.log(numbers3);

// Создаем массивы с именами и возрастами
let names = ["Семен", "Иван", "Петр", "Татьяна"];
let ages = [18, 27, 74, 34];

// Создаем новый массив для заполнения строками по образу "Имя Возраст лет/годов"
let combinedArray = names.map((name, i) => {
  let age = ages[i];
  return `${name} ${age} лет/годов`;
});

console.log(combinedArray);

// Заполняем новый массив при помощи цикла

// Выводим полученный массив в консоль

let originalArray = [1, 2, 3, 4, 5]; // Предположим, что это ваш исходный массив

// Используем метод reverse() для обращения порядка элементов в массиве

let newArray = originalArray.reverse();

// Вывод обращенного массива в консоль

console.log(newArray);
